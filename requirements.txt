fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
openai>=1.30.0,<2.0
python-dotenv>=1.0.0,<2.0
jinja2>=3.1.3,<4.0
aiofiles>=23.2.0,<24.0
pydantic>=2.6.0,<3.0
httpx>=0.27.0,<1.0
python-multipart>=0.0.9,<1.0
# File upload extraction
python-docx>=1.1.0,<2.0
python-pptx>=1.0.0,<2.0
# PDF parsing (research reports / filings): MarkItDown primary + PyMuPDF fallback
markitdown>=0.1.7
pymupdf>=1.24.0
# HTML parsing (broker reports, site crawler, web search)
beautifulsoup4>=4.12.0
# PDF export
weasyprint>=60.0
# Markdown rendering
markdown-it-py>=3.0.0
# Market data sources (free, no API key required)
akshare>=1.12.0
yfinance>=0.2.36
# 表格解析：.xlsx 用 openpyxl，旧版 .xls 用 xlrd（xlrd 2.x 只支持 .xls）
# 两者原先靠 akshare 间接带入；显式声明，避免 akshare 变更/移除时上传接口静默失效
openpyxl>=3.1.0,<4.0
xlrd>=2.0.1,<3.0
